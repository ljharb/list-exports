/**
 *
 * @param {import('./constructRoutes').ResolvedRoutesConfig} resolvedRoutesConfig
 * @param {string} path
 * @returns {import('./constructRoutes').ResolvedRoutesConfig}
 */
export function matchRoute(resolvedRoutesConfig: import('./constructRoutes').ResolvedRoutesConfig, pathMatch: any): import('./constructRoutes').ResolvedRoutesConfig;
export function resolvePath(prefix: any, path: any): any;
